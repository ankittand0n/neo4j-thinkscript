# Enable auto indexing
dbms.index.default_schema_providers=lucene+native-3.0
dbms.index.default_lookup_index_providers=lucene+native-3.0

# Configure auto indexing for specific properties
dbms.index.auto.enable=true
dbms.index.auto.enable_for_labels=Node,ContentChunk
dbms.index.auto.enable_for_properties=title,content

# Configure fulltext index
dbms.index.fulltext.enable=true
dbms.index.fulltext.analyzer=standard
dbms.index.fulltext.eventually_consistent=true 